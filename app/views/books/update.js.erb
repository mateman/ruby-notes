<% if @book.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @book.id %>').replaceWith(' <%= j render "book", book: @book %>');
  $('.alert').html("El cuaderno se ha modificado con éxito").show();
  $('#form-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @book.errors.full_messages[0] %>');
<% end %>
